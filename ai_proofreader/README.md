# AI Proofreader

What it does
- Proofreads and suggests grammar/style edits for input text.

Setup
1. Install dependencies: `pip install -r requirements.txt`.
2. Run it:

```powershell
cd ai_proofreader\ai_proofreader
uvicorn app:app --reload --port 8005
```
