# Medical AI Symptom Checker

What it does
- Basic symptom checker demo for educational purposes.

Setup
1. Install dependencies: `pip install -r requirements.txt`.
2. Run:

```powershell
cd medical_ai_symptom_checker\medical_ai_symptom_checker
uvicorn app:app --reload --port 8009
```

Notes
- Not a replacement for professional medical advice.
