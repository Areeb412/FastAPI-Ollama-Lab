# AI News Summarizer

What it does
- Summarizes news articles into brief bullet points.

Setup & Usage
1. Install dependencies: `pip install -r requirements.txt`.
2. Start the app:

```powershell
cd ai_news_summarizer\ai_news_summarizer
uvicorn app:app --reload --port 8004
```
