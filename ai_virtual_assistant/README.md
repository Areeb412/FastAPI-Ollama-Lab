# AI Virtual Assistant

What it does
- Minimal assistant interface for short queries and actions.

Setup
1. Install dependencies: `pip install -r requirements.txt`.
2. Run:

```powershell
cd ai_virtual_assistant\ai_virtual_assistant
uvicorn app:app --reload --port 8006
```
