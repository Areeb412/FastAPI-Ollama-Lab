# Ecommerce AI Recommender

What it does
- Small recommender demo showcasing product suggestions.

Setup
1. Install dependencies: `pip install -r requirements.txt`.
2. Run:

```powershell
cd ecommerce_ai_recommender\ecommerce_ai_recommender
uvicorn app:app --reload --port 8008
```
