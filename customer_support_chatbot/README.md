# Customer Support Chatbot

What it does
- Demonstration chatbot that answers common support questions.

Setup
1. Install dependencies: `pip install -r requirements.txt`.
2. Run:

```powershell
cd customer_support_chatbot\customer_support_chatbot
uvicorn app:app --reload --port 8007
```
