# AI Legal Analyzer

What it does
- Basic legal text analyzer and clause summarizer demo.

Setup
1. Install dependencies: `pip install -r requirements.txt`.
2. Run the app:

```powershell
cd ai_legal_analyzer\ai_legal_analyzer
uvicorn app:app --reload --port 8003
```

Notes
- Designed as a demo only; not legal advice.
